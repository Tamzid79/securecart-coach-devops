global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: "securecart-coach"
    metrics_path: /metrics
    static_configs:
      - targets: ["securecart-staging:3000"]
